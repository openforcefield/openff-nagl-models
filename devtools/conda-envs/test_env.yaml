name: openff-nagl-models-test

channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - platformdirs
  - versioningit

    # Testing
  - pytest
  - pytest-socket
  - pytest-randomly
  - openff-nagl-base >=0.5.2

  - # Type-checking
  - mypy
