name: openff-nagl-models-test

channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - importlib_resources

    # Testing
  - pytest
  - pytest-socket
  - pytest-randomly
  - openff-nagl-base >=0.4.0

